<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.inspiron.jibei">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity
                android:name=".ui.MainActivity"
                android:label="@string/title_activity_main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.AddBillActivity">
        </activity>
        <activity android:name=".ui.BillBudgetSetting">
        </activity>
        <activity android:name=".ui.BillNoting">
        </activity>
        <activity android:name=".ui.ConnectUsActivity">
        </activity>
        <activity android:name=".ui.InfoActivity">
        </activity>
    </application>

</manifest>